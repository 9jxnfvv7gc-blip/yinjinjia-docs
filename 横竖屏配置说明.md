# 📱 横竖屏配置说明

## ✅ 当前配置状态

### iOS配置（已配置完成）

**文件位置**：`ios/Runner/Info.plist`

**配置内容**：
```xml
<key>UISupportedInterfaceOrientations</key>
<array>
    <string>UIInterfaceOrientationPortrait</string>        <!-- 竖屏 -->
    <string>UIInterfaceOrientationLandscapeLeft</string>   <!-- 横屏左 -->
    <string>UIInterfaceOrientationLandscapeRight</string>  <!-- 横屏右 -->
</array>
```

**支持的屏幕方向**：
- ✅ 竖屏（Portrait）
- ✅ 横屏左（LandscapeLeft）
- ✅ 横屏右（LandscapeRight）

### Android配置（已配置完成）

**文件位置**：`android/app/src/main/AndroidManifest.xml`

**配置内容**：
```xml
<activity
    android:name=".MainActivity"
    android:screenOrientation="unspecified"
    android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
    ...>
```

**配置说明**：
- `android:screenOrientation="unspecified"` - 允许所有方向（横竖屏）
- `android:configChanges="orientation|..."` - 方向变化时不重启Activity

---

## 🎯 支持的屏幕方向

### iPhone/iPad
- ✅ **竖屏**（Portrait）
- ✅ **横屏左**（LandscapeLeft）
- ✅ **横屏右**（LandscapeRight）
- ✅ **iPad倒置竖屏**（PortraitUpsideDown，仅iPad）

### Android
- ✅ **竖屏**（Portrait）
- ✅ **横屏**（Landscape - 左右都支持）
- ✅ **自动旋转**（根据设备方向自动切换）

---

## 🔧 如何修改配置

### 如果只想支持竖屏

**iOS** - 修改 `ios/Runner/Info.plist`：
```xml
<key>UISupportedInterfaceOrientations</key>
<array>
    <string>UIInterfaceOrientationPortrait</string>
</array>
```

**Android** - 修改 `android/app/src/main/AndroidManifest.xml`：
```xml
android:screenOrientation="portrait"
```

### 如果只想支持横屏

**iOS** - 修改 `ios/Runner/Info.plist`：
```xml
<key>UISupportedInterfaceOrientations</key>
<array>
    <string>UIInterfaceOrientationLandscapeLeft</string>
    <string>UIInterfaceOrientationLandscapeRight</string>
</array>
```

**Android** - 修改 `android/app/src/main/AndroidManifest.xml`：
```xml
android:screenOrientation="landscape"
```

### 如果支持所有方向（当前配置）

**iOS** - 保持当前配置即可

**Android** - 使用 `android:screenOrientation="unspecified"`（已配置）

---

## 📝 Flutter代码中的方向控制

### 在代码中锁定方向

```dart
import 'package:flutter/services.dart';

// 锁定为竖屏
SystemChrome.setPreferredOrientations([
  DeviceOrientation.portraitUp,
]);

// 锁定为横屏
SystemChrome.setPreferredOrientations([
  DeviceOrientation.landscapeLeft,
  DeviceOrientation.landscapeRight,
]);

// 允许所有方向
SystemChrome.setPreferredOrientations([
  DeviceOrientation.portraitUp,
  DeviceOrientation.portraitDown,
  DeviceOrientation.landscapeLeft,
  DeviceOrientation.landscapeRight,
]);
```

### 在特定页面锁定方向

```dart
class VideoPlayerPage extends StatefulWidget {
  @override
  _VideoPlayerPageState createState() => _VideoPlayerPageState();
}

class _VideoPlayerPageState extends State<VideoPlayerPage> {
  @override
  void initState() {
    super.initState();
    // 进入页面时锁定为横屏
    SystemChrome.setPreferredOrientations([
      DeviceOrientation.landscapeLeft,
      DeviceOrientation.landscapeRight,
    ]);
  }

  @override
  void dispose() {
    // 离开页面时恢复所有方向
    SystemChrome.setPreferredOrientations([
      DeviceOrientation.portraitUp,
      DeviceOrientation.portraitDown,
      DeviceOrientation.landscapeLeft,
      DeviceOrientation.landscapeRight,
    ]);
    super.dispose();
  }
}
```

---

## 🧪 测试方法

### iOS测试
1. 在模拟器或真机上运行应用
2. 旋转设备（或使用模拟器的旋转按钮）
3. 检查应用是否正常旋转

### Android测试
1. 在模拟器或真机上运行应用
2. 旋转设备（或使用模拟器的旋转按钮）
3. 检查应用是否正常旋转

---

## ✅ 配置检查清单

- [x] iOS Info.plist 已配置横竖屏支持
- [x] Android AndroidManifest.xml 已配置横竖屏支持
- [x] Android configChanges 包含 orientation
- [x] Android screenOrientation 设置为 unspecified

---

**当前配置：支持横竖屏自动旋转！** ✅


