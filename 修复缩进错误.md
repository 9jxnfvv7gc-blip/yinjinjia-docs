# 修复缩进错误

## ❌ 错误信息

```
IndentationError: unexpected indent
```

sed命令添加的代码缩进不正确。

---

## 🔧 修复步骤

### 步骤1：恢复备份文件

在Workbench终端执行：

```bash
cd /root/video_server

# 恢复备份文件
cp video_server.py.bak video_server.py

# 或者如果没有备份，从git恢复（如果有）
```

---

### 步骤2：使用正确缩进的命令

在Workbench终端执行：

```bash
cd /root/video_server

# 备份原文件
cp video_server.py video_server.py.bak

# 在第626行之后添加调试信息（正确缩进：12个空格）
sed -i '626a\            # 调试信息\n            print(f"API列表请求: category={category}, category_path={category_path}")\n            print(f"VIDEO_ROOT: {VIDEO_ROOT}")\n            print(f"路径存在: {os.path.exists(category_path)}")' video_server.py

# 在第638行之后添加（正确缩进：16个空格，在if语句块内）
sed -i '639a\                print(f"目录存在，开始扫描文件...")' video_server.py

# 在第644行之后添加（正确缩进：24个空格，在for循环内）
sed -i '645a\                        print(f"文件: {f}, 扩展名: {ext}, 在列表中: {ext in all_media_extensions}")' video_server.py

# 在第676行之后添加（正确缩进：16个空格，在if语句块内）
sed -i '677a\                print(f"找到 {len(file_list)} 个文件")' video_server.py
```

---

## 🎯 更简单的方法：直接编辑文件

### 在Workbench终端执行：

```bash
cd /root/video_server

# 恢复备份
cp video_server.py.bak video_server.py

# 打开编辑器
nano video_server.py
```

### 然后找到第638行左右（`if os.path.exists(category_path):`），在这一行下面添加：

```python
                print(f"目录存在，开始扫描文件...")
```

**注意**：缩进是16个空格（4个tab或对齐到if语句块内）

### 找到第644行左右（`if ext in all_media_extensions:`），在这一行上面添加：

```python
                        print(f"文件: {f}, 扩展名: {ext}, 在列表中: {ext in all_media_extensions}")
```

**注意**：缩进是24个空格（6个tab或对齐到for循环内）

### 找到第676行左右（`file_list.append`循环结束后），添加：

```python
                print(f"找到 {len(file_list)} 个文件")
```

**注意**：缩进是16个空格（4个tab或对齐到if语句块内）

### 保存文件：
- 按 `Ctrl+O` 保存
- 按 `Enter` 确认
- 按 `Ctrl+X` 退出

---

## 🚀 推荐操作

**最简单的方法**：直接编辑文件，手动添加3行代码，注意缩进。

---

**先恢复备份，然后手动添加代码！** 🔧

