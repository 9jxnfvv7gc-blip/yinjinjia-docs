# 创建目录后的下一步操作

## ✅ 如果目录创建成功

你应该看到类似这样的输出：

```
root@iZj6cg78ov73x6cxbephc1Z:~/video_server# mkdir -p /root/video_server
root@iZj6cg78ov73x6cxbephc1Z:~/video_server# mkdir -p /root/videos/原创视频
root@iZj6cg78ov73x6cxbephc1Z:~/video_server# mkdir -p /root/videos/原创歌曲
root@iZj6cg78ov73x6cxbephc1Z:~/video_server# ls -la /root/
total XX
drwx------ 17 root root 4096 Nov 23 XX:XX ..
drwxr-xr-x  2 root root 4096 Nov 23 XX:XX video_server
drwxr-xr-x  3 root root 4096 Nov 23 XX:XX videos
...
```

---

## 🔍 步骤1：验证目录已创建

在Workbench终端执行：

```bash
# 检查 /root/ 目录
ls -la /root/

# 检查 video_server 目录
ls -la /root/video_server/

# 检查 videos 目录结构
ls -la /root/videos/
ls -la /root/videos/原创视频/
ls -la /root/videos/原创歌曲/
```

**应该看到**：
- `/root/video_server/` 目录存在
- `/root/videos/原创视频/` 目录存在
- `/root/videos/原创歌曲/` 目录存在

---

## 📤 步骤2：检查 video_server.py 是否已存在

在Workbench终端执行：

```bash
# 检查 video_server.py 是否已上传
ls -la /root/video_server/video_server.py
```

**如果文件存在**：
- ✅ 说明之前已经上传过了
- ✅ 可以跳过上传步骤

**如果文件不存在**：
- ⏳ 需要上传 `video_server.py` 文件

---

## 📤 步骤3：上传 video_server.py（如果还没有）

### 方法1：使用Workbench文件管理器（如果可用）

1. 打开Workbench文件管理器
2. 导航到 `/root/video_server/` 目录
3. 点击**上传**按钮
4. 选择本地的 `video_server.py` 文件
5. 等待上传完成

### 方法2：使用scp从Mac上传（推荐）

**在你的Mac电脑终端执行**：

```bash
# 上传 video_server.py 到服务器
scp "/Volumes/Expansion/FlutterProjects/桌面影音播放器_安装包_20251121_165905/video_server.py" root@47.243.177.166:/root/video_server/
```

**如果SSH密码登录失败**，使用Workbench文件管理器上传。

---

## ✅ 步骤4：验证服务器配置

在Workbench终端执行：

```bash
# 检查 video_server.py 是否存在
ls -la /root/video_server/video_server.py

# 检查配置（VIDEO_ROOT应该是 /root/videos）
grep "VIDEO_ROOT" /root/video_server/video_server.py
```

**应该显示**：`VIDEO_ROOT = "/root/videos"`

**如果不是**，需要修改：

```bash
cd /root/video_server
sed -i 's|VIDEO_ROOT = "/Volumes/Expansion"|VIDEO_ROOT = "/root/videos"|g' video_server.py
grep "VIDEO_ROOT" video_server.py
```

---

## 🧪 步骤5：测试服务器

在Workbench终端执行：

```bash
# 测试API（应该返回空数组）
curl http://localhost:8081/api/list/原创视频
curl http://localhost:8081/api/list/原创歌曲
```

**应该返回**：`[]`（空数组，因为没有内容）

---

## 📋 完整操作流程

### 在Workbench终端执行：

```bash
# 1. 验证目录
ls -la /root/
ls -la /root/video_server/
ls -la /root/videos/

# 2. 检查 video_server.py
ls -la /root/video_server/video_server.py

# 3. 如果文件不存在，需要上传（使用Workbench文件管理器或scp）

# 4. 检查配置
grep "VIDEO_ROOT" /root/video_server/video_server.py

# 5. 测试服务器
curl http://localhost:8081/api/list/原创视频
```

---

## 🎯 现在可以执行

1. **验证目录已创建**（`ls -la /root/`）
2. **检查 video_server.py 是否存在**（`ls -la /root/video_server/video_server.py`）
3. **如果不存在，上传文件**
4. **测试服务器**（`curl http://localhost:8081/api/list/原创视频`）

---

**先执行验证命令，告诉我结果！** 🔍

