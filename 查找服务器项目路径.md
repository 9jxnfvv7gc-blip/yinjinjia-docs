# 🔍 查找服务器上Flutter项目路径

## 📋 步骤

### 步骤1：SSH连接到服务器

```bash
ssh root@47.243.177.166
```

### 步骤2：查找Flutter项目

在服务器上运行以下命令查找项目：

```bash
# 方法1：查找包含pubspec.yaml的目录（Flutter项目标识）
find /root -name "pubspec.yaml" 2>/dev/null

# 方法2：查找包含AndroidManifest.xml的目录
find /root -name "AndroidManifest.xml" -path "*/android/app/src/main/*" 2>/dev/null

# 方法3：查找包含main.dart的目录
find /root -name "main.dart" -path "*/lib/*" 2>/dev/null

# 方法4：查看常见目录
ls -la /root/
ls -la /root/app/
ls -la /root/flutter/
ls -la /root/video_server/
```

### 步骤3：确认项目路径

找到项目后，确认路径，例如：
- `/root/app`
- `/root/flutter/video_music_app`
- `/root/video_music_app`
- 或其他路径

### 步骤4：上传文件到正确路径

找到路径后，在本地Mac运行：

```bash
cd /Volumes/Expansion/FlutterProjects/桌面影音播放器_安装包_20251121_165905
scp android/app/src/main/AndroidManifest.xml root@47.243.177.166:/正确的项目路径/android/app/src/main/AndroidManifest.xml
```

---

**请先SSH到服务器，运行查找命令，告诉我找到的路径！**


