# 上传文件到北京服务器

## ⚠️ 当前状态

- ✅ 目录已创建
- ✅ 服务已配置（但文件不存在，所以服务会失败）
- ❌ `video_server.py` 文件还未上传

---

## 📤 上传文件

### 方法1：使用scp（推荐）

在你的Mac终端执行：

```bash
scp "/Volumes/Expansion/FlutterProjects/桌面影音播放器_安装包_20251121_165905/video_server.py" admin@39.107.137.136:/tmp/
```

然后在服务器上移动文件：

```bash
sudo mv /tmp/video_server.py /root/video_server/
sudo chown root:root /root/video_server/video_server.py
```

---

### 方法2：使用Workbench文件管理器

1. **在Workbench中**，点击"文件管理器"图标
2. **导航到** `/root/video_server/`（可能需要sudo权限）
3. **上传** `video_server.py` 文件

---

### 方法3：直接创建文件（如果上传困难）

在服务器上直接创建文件内容（比较复杂，不推荐）

---

## 🔧 上传后配置

文件上传后，执行：

```bash
# 1. 修改VIDEO_ROOT
sudo sed -i '20s|VIDEO_ROOT = "/Volumes/Expansion"|VIDEO_ROOT = "/root/videos"|g' /root/video_server/video_server.py

# 2. 确认配置
sudo grep -n "VIDEO_ROOT" /root/video_server/video_server.py | head -1

# 3. 重启服务
sudo systemctl restart video-server-beijing

# 4. 检查服务状态
sudo systemctl status video-server-beijing
```

---

## 🛡️ 关于防火墙

对于 `Command may disrupt existing ssh connections. Proceed with operation (y|n)?`

**选择 `y`（是）**

原因：
- ✅ 我们已经添加了8081端口规则
- ✅ SSH端口（22）通常默认允许
- ✅ 即使SSH断开，Workbench应该还能用
- ✅ 如果断开，可以重新连接

**如果担心断开，可以先确保SSH端口已开放**：

```bash
# 先确保SSH端口开放
sudo ufw allow 22/tcp

# 然后启用防火墙
sudo ufw enable
```

---

## 📋 执行顺序

1. **先上传文件**（使用scp或Workbench）
2. **配置VIDEO_ROOT**
3. **重启服务**
4. **处理防火墙**（选择y）

---

**先上传文件，然后告诉我结果！** 📤

