# 🚀 在服务器上执行 - 完整步骤

## 📋 当前状态
- ✅ 已连接到服务器（admin@iZ2ze8pmrf4dio8qff73jvZ）
- ⏳ 需要上传文件并启动服务器

## 步骤1：检查文件是否已上传

在服务器终端执行：

```bash
ls -la /tmp/video_server.py
```

### 如果文件存在：
继续步骤2

### 如果文件不存在：
**在本地 Mac 打开新终端窗口**，执行：

```bash
cd "/Volumes/Expansion/FlutterProjects/桌面影音播放器_安装包_20251121_165905"
scp video_server.py admin@39.107.137.136:/tmp/video_server.py
```

输入密码后，回到服务器终端继续。

## 步骤2：在服务器上执行（全部复制粘贴）

```bash
# 创建目录
mkdir -p ~/app
mkdir -p ~/videos/原创视频
mkdir -p ~/videos/原创歌曲

# 移动文件
mv /tmp/video_server.py ~/app/
chmod +x ~/app/video_server.py

# 检查文件
ls -la ~/app/video_server.py

# 启动服务器
cd ~/app
export VIDEO_ROOT="$HOME/videos"
nohup python3 video_server.py > ~/server.log 2>&1 &

# 等待2秒
sleep 2

# 检查进程
ps aux | grep video_server.py

# 查看日志
tail -30 ~/server.log

# 测试 API
curl http://localhost:8081/api/categories
curl http://localhost:8081/api/list/原创视频
```

## ✅ 如果看到 JSON 数据，说明服务器启动成功！

然后在 iPhone 上刷新应用，就能看到视频了。


