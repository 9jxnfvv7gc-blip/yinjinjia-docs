# 🔄 恢复原配置和视频文件

## 📋 根据提交记录 7653dd5 的配置

- **服务器地址**：`47.243.177.166:8081`（香港服务器）
- **视频目录**：`/root/videos/`
- **功能**：分享、删除功能都已设置好

## 🔍 在香港服务器上查找视频文件

### 在服务器终端执行：

```bash
# 查找所有视频文件
find /root/videos -type f \( -name "*.mp4" -o -name "*.mov" -o -name "*.mkv" \) 2>/dev/null

# 检查目录结构
ls -laR /root/videos/

# 检查其他可能的位置
find /root -type d -name "*video*" -o -name "*视频*" 2>/dev/null
```

## ✅ 如果找到视频文件

如果视频在 `/root/videos/原创视频/`：

```bash
# 确认服务器正在运行
ps aux | grep video_server.py

# 确认 VIDEO_ROOT 设置正确
export VIDEO_ROOT="/root/videos"

# 测试 API
curl http://localhost:8081/api/list/原创视频
```

## 🔧 如果视频在其他位置

例如在 `/root/my_videos/`：

```bash
# 停止旧服务器
pkill -f video_server.py

# 修改 VIDEO_ROOT
export VIDEO_ROOT="/root/my_videos"

# 重新启动
cd /root/app
nohup python3 video_server.py > /root/server.log 2>&1 &

# 测试
curl http://localhost:8081/api/list/原创视频
```

## 📱 应用配置

应用已经配置为使用香港服务器，不需要修改。

只需要：
1. 找到视频文件位置
2. 确保服务器 VIDEO_ROOT 指向正确位置
3. 重启服务器
4. 在 iPhone 上刷新应用

## 🎯 恢复分享、删除功能

一旦视频文件找到并正确配置，分享、删除功能会自动恢复，因为这些功能已经在代码中实现了。


