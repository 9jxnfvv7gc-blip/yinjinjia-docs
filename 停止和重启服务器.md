# 停止和重启服务器

## 🛑 停止服务器

### 在Workbench终端执行：

```bash
# 停止服务器
systemctl stop video-server

# 检查服务状态（应该显示 inactive）
systemctl status video-server
```

---

## 🔄 重启服务器

### 在Workbench终端执行：

```bash
# 重启服务器
systemctl restart video-server

# 检查服务状态（应该显示 active (running)）
systemctl status video-server
```

---

## 📋 完整操作流程

### 1. 停止服务器
```bash
systemctl stop video-server
```

### 2. 编辑文件
```bash
nano /root/video_server/video_server.py
```

### 3. 添加调试信息（注意缩进）

### 4. 保存文件（Ctrl+O, Enter, Ctrl+X）

### 5. 检查语法
```bash
python3 -m py_compile /root/video_server/video_server.py
```

### 6. 启动服务器
```bash
systemctl start video-server
```

### 7. 检查服务状态
```bash
systemctl status video-server
```

---

## 🎯 现在可以执行

1. **停止服务器**（`systemctl stop video-server`）
2. **编辑文件**（`nano /root/video_server/video_server.py`）
3. **添加调试信息**
4. **保存文件**
5. **启动服务器**（`systemctl start video-server`）

---

**先停止服务器，然后编辑文件！** 🛑

