# 📤 上传 video_server.py 到服务器 - 手动方法

## 方法1：使用 scp（需要密码）

在本地 Mac 终端执行：

```bash
cd "/Volumes/Expansion/FlutterProjects/桌面影音播放器_安装包_20251121_165905"
scp video_server.py admin@39.107.137.136:/tmp/video_server.py
```

然后输入密码。

## 方法2：在服务器上直接创建文件（推荐）

### 步骤1：在本地 Mac 查看文件内容

```bash
cd "/Volumes/Expansion/FlutterProjects/桌面影音播放器_安装包_20251121_165905"
head -50 video_server.py
```

### 步骤2：在服务器上创建文件

在服务器上执行：

```bash
# 创建文件
nano /root/app/video_server.py
```

然后：
1. 复制整个 `video_server.py` 文件内容
2. 粘贴到 nano 编辑器
3. 按 `Ctrl + O` 保存
4. 按 `Ctrl + X` 退出

## 方法3：使用阿里云文件上传功能

1. 登录阿里云控制台
2. 进入 ECS → 文件管理
3. 上传 `video_server.py` 到 `/root/app/` 目录

## 方法4：使用 wget 或 curl（如果文件在可访问的位置）

如果文件在 GitHub 或其他可访问的位置，可以使用：

```bash
wget https://文件地址/video_server.py -O /root/app/video_server.py
```


