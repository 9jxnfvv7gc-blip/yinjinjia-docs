# 🔍 检查北京服务器8082端口状态

## ❌ 当前问题

应用显示"网络异常，无法连接到服务器"。

## 🔍 需要检查的事项

### 1. 检查服务器上的Flask服务是否运行

**在服务器上执行**（通过SSH或Workbench）：

```bash
# 检查8082端口是否在监听
ss -tlnp | grep 8082

# 或使用
netstat -tlnp | grep 8082

# 检查进程
ps aux | grep server.py | grep 8082
```

**如果看到输出**：
- ✅ 服务正在运行
- 应该看到类似：`LISTEN 0 5 0.0.0.0:8082`

**如果没有输出**：
- ❌ 服务没有运行，需要启动

---

### 2. 启动链接服务器（如果未运行）

**在服务器上执行**：

```bash
# 进入链接服务器目录
cd ~/link_server

# 检查server.py是否存在
ls -la server.py

# 启动服务器（前台运行，用于测试）
python3 server.py

# 应该看到：
# * Running on all addresses (0.0.0.0)
# * Running on http://127.0.0.1:8082
# * Running on http://172.16.18.12:8082
```

**如果要在后台运行**：

```bash
# 使用nohup在后台运行
cd ~/link_server
nohup python3 server.py > server.log 2>&1 &

# 检查是否启动成功
ps aux | grep server.py
```

---

### 3. 检查防火墙是否开放8082端口

**在阿里云控制台操作**：

1. 登录阿里云控制台
2. 进入 **ECS实例** → 选择北京服务器（IP: 39.107.137.136）
3. 点击 **防火墙** 或 **安全组**
4. 检查是否有 **8082端口** 的入站规则
5. 如果没有，点击 **添加规则**：
   - **协议**：`TCP`
   - **端口范围**：`8082`
   - **来源IP**：`0.0.0.0/0`（允许所有IP访问）
   - **备注**：`链接服务器`
   - 点击 **保存**

---

### 4. 测试服务器连接

**在Mac上执行**：

```bash
# 测试8082端口是否可访问
curl -v --connect-timeout 5 http://39.107.137.136:8082/api/categories

# 如果成功，应该看到JSON数据：
# [{"id":"video","name":"原创视频"}]
```

**如果连接超时**：
- 检查防火墙是否开放
- 检查服务器服务是否运行

**如果连接被拒绝**：
- 服务可能没有运行
- 或服务只监听本地（127.0.0.1）

---

### 5. 检查server.py配置

**在服务器上检查**：

```bash
cd ~/link_server
cat server.py | grep -A 5 "app.run"
```

**应该看到**：
```python
app.run(host="0.0.0.0", port=8082)
```

**如果是** `host="127.0.0.1"`：
- ❌ 只监听本地，需要改为 `0.0.0.0`

---

## ✅ 快速检查清单

- [ ] 服务器8082端口是否在监听？
- [ ] Flask服务是否正在运行？
- [ ] 防火墙是否开放8082端口？
- [ ] server.py是否监听0.0.0.0？
- [ ] 从Mac能否访问 http://39.107.137.136:8082？

---

## 🔧 如果所有检查都正常，但仍然无法连接

1. **检查服务器日志**：
   ```bash
   cd ~/link_server
   tail -f server.log
   # 或如果使用nohup
   tail -f nohup.out
   ```

2. **检查links.json文件**：
   ```bash
   cd ~/link_server
   cat links.json
   # 确保文件格式正确，有video分类
   ```

3. **重启服务器**：
   ```bash
   # 停止旧进程
   pkill -f "python3 server.py"
   
   # 重新启动
   cd ~/link_server
   nohup python3 server.py > server.log 2>&1 &
   ```

---

## 📱 安装新APK测试

新APK已构建完成，包含8082端口配置：

```bash
# 在Mac上执行
cd "/Volumes/Expansion/FlutterProjects/桌面影音播放器_安装包_20251121_165905"
adb devices  # 确认设备已连接
adb install -r backups/apk_20251201/app-domestic-release-8082.apk
```

安装后，打开应用测试连接。

