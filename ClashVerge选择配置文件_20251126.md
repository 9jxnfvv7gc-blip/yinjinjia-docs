# 📁 Clash Verge 选择配置文件（2025-11-26）

## 🎯 选择配置文件

点击 "choose file" 后，需要从电脑里选择一个文件。

---

## ✅ 操作步骤

### 方法1：选择已创建的配置文件（推荐）

我们之前已经通过终端创建了配置文件，可以直接选择它。

#### 步骤1：打开文件选择对话框

1. **点击 "choose file" 按钮**
2. **会打开文件选择对话框**

#### 步骤2：导航到配置文件夹

1. **按 `Command + Shift + G`**（快速跳转到路径）
2. **输入以下路径**：
   ```
   ~/Library/Application Support/clash-verge/profiles/
   ```
3. **按回车**

#### 步骤3：选择配置文件

1. **找到 `hongkong.yaml` 文件**
2. **点击选择**
3. **点击"打开"**

---

### 方法2：如果文件不存在，创建新文件

如果找不到 `hongkong.yaml` 文件，可以：

#### 步骤1：创建配置文件

1. **在文件选择对话框中**
2. **导航到**：`~/Library/Application Support/clash-verge/profiles/`
3. **如果文件夹不存在，先创建它**

#### 步骤2：创建新文件

1. **在文件选择对话框中**
2. **右键点击空白处**
3. **选择"新建文档"或"New Document"**
4. **命名为**：`hongkong.yaml`
5. **选择这个文件**

#### 步骤3：编辑配置文件

1. **选择文件后，会打开编辑器**
2. **粘贴配置内容**：

```yaml
port: 7890
socks-port: 7891
allow-lan: true
mode: rule
log-level: info

proxies:
  - name: "HongKong-Squid"
    type: http
    server: 47.243.177.166
    port: 3128

proxy-groups:
  - name: "Proxy"
    type: select
    proxies:
      - "HongKong-Squid"

rules:
  - MATCH,Proxy
```

3. **保存文件**

---

## 🔍 快速导航方法

### 方法1：使用快捷键

1. **点击 "choose file"**
2. **按 `Command + Shift + G`**（快速跳转）
3. **输入路径**：`~/Library/Application Support/clash-verge/profiles/`
4. **按回车**
5. **选择 `hongkong.yaml`**

### 方法2：手动导航

1. **点击 "choose file"**
2. **在左侧边栏找到"用户"或"Users"**
3. **进入你的用户文件夹**
4. **进入 Library 文件夹**（可能需要显示隐藏文件：`Command + Shift + .`）
5. **进入 Application Support 文件夹**
6. **进入 clash-verge 文件夹**
7. **进入 profiles 文件夹**
8. **选择 `hongkong.yaml`**

---

## 📝 如果找不到文件

### 重新创建配置文件

在终端运行：

```bash
# 创建配置文件夹
mkdir -p ~/Library/Application\ Support/clash-verge/profiles/

# 创建配置文件
cat > ~/Library/Application\ Support/clash-verge/profiles/hongkong.yaml << 'EOF'
port: 7890
socks-port: 7891
allow-lan: true
mode: rule
log-level: info

proxies:
  - name: "HongKong-Squid"
    type: http
    server: 47.243.177.166
    port: 3128

proxy-groups:
  - name: "Proxy"
    type: select
    proxies:
      - "HongKong-Squid"

rules:
  - MATCH,Proxy
EOF
```

然后重新在 Clash Verge 中选择文件。

---

## ✅ 现在请操作

1. **点击 "choose file"**
2. **按 `Command + Shift + G`**
3. **输入路径**：`~/Library/Application Support/clash-verge/profiles/`
4. **选择 `hongkong.yaml` 文件**
5. **点击"打开"**

如果找不到文件，告诉我，我可以帮你重新创建。

---

**最后更新**：2025-11-26








