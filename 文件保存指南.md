# 💾 文件保存指南 - 快速找到重要文件

## 🎯 快速访问方法

### 方法1：使用Git标签（最推荐）

```bash
# 1. 创建标签（保存当前版本）
git tag 测试版本_20251124

# 2. 查看所有标签
git tag

# 3. 切换到标签版本
git checkout 测试版本_20251124

# 4. 查看标签信息
git show 测试版本_20251124
```

### 方法2：使用Git分支

```bash
# 1. 创建测试分支
git checkout -b 测试分支_20251124

# 2. 提交当前更改
git add .
git commit -m "测试版本：Android和iOS测试配置"

# 3. 查看所有分支
git branch -a

# 4. 切换分支
git checkout 测试分支_20251124
```

### 方法3：创建书签文件（最简单）

我已经为你创建了 `快速测试指南_20251124.md`，包含：
- Android测试步骤
- iOS测试步骤
- 快速命令
- 重要文件位置

**快速找到**：
```bash
cd /Volumes/Expansion/FlutterProjects/桌面影音播放器_安装包_20251121_165905
open 快速测试指南_20251124.md
```

### 方法4：使用Cursor工作区

1. **保存工作区**：
   - Cursor → File → Save Workspace As...
   - 文件名：`video_app.code-workspace`
   - 保存位置：项目根目录

2. **下次打开**：
   - Cursor → File → Open Workspace
   - 选择 `video_app.code-workspace`

### 方法5：创建快捷脚本

```bash
# 创建快速访问脚本
cat > ~/open_video_app.sh << 'EOF'
#!/bin/bash
cd /Volumes/Expansion/FlutterProjects/桌面影音播放器_安装包_20251121_165905
open -a Cursor .
EOF

chmod +x ~/open_video_app.sh
```

使用：在终端运行 `~/open_video_app.sh`

---

## 📁 重要文件位置速查

### 核心代码
- `lib/main.dart` - 应用入口
- `lib/config.dart` - 服务器配置
- `lib/simple_home_page.dart` - 主界面

### 配置文件
- `pubspec.yaml` - 依赖配置
- `ios/Runner.xcworkspace` - iOS项目
- `android/app/build.gradle.kts` - Android配置

### 测试指南
- `快速测试指南_20251124.md` - **最新测试指南**
- `真实Android设备测试指南.md` - Android详细指南
- `iOS测试指南.md` - iOS详细指南

### Git版本
- 当前版本：`fb56977` (2025-11-23)
- 查看：`git show fb56977`
- 切换：`git checkout fb56977`

---

## 🚀 快速命令速查

### 进入项目
```bash
cd /Volumes/Expansion/FlutterProjects/桌面影音播放器_安装包_20251121_165905
```

### Android测试
```bash
adb devices
flutter run -d android
```

### iOS测试
```bash
flutter devices
flutter run -d ios
```

### Git操作
```bash
# 查看状态
git status

# 查看历史
git log --oneline -10

# 创建标签
git tag 测试版本_20251124

# 切换版本
git checkout fb56977
```

---

## 💡 建议

1. **每次重要更改后创建Git标签**：
   ```bash
   git tag 测试版本_$(date +%Y%m%d)
   ```

2. **定期提交代码**：
   ```bash
   git add .
   git commit -m "描述你的更改"
   ```

3. **使用书签文件**：
   - 重要信息保存在 `快速测试指南_20251124.md`
   - 每次更新后更新这个文件

4. **保存工作区**：
   - 在Cursor中保存工作区，下次直接打开

---

**创建日期：2025-11-24**
**项目路径：/Volumes/Expansion/FlutterProjects/桌面影音播放器_安装包_20251121_165905**

