# 🖥️ 服务器编译APK完整步骤

## ✅ 已找到项目路径

- **项目路径**：`/root/app`
- **AndroidManifest.xml路径**：`/root/app/android/app/src/main/AndroidManifest.xml`

## 🚀 完整步骤

### 步骤1：上传AndroidManifest.xml（已完成）

```bash
cd /Volumes/Expansion/FlutterProjects/桌面影音播放器_安装包_20251121_165905
scp android/app/src/main/AndroidManifest.xml root@47.243.177.166:/root/app/android/app/src/main/AndroidManifest.xml
```

### 步骤2：SSH连接到服务器

```bash
ssh root@47.243.177.166
```

### 步骤3：进入项目目录并编译

```bash
# 进入项目目录
cd /root/app

# 获取依赖
flutter pub get

# 编译Debug版本APK
flutter build apk --debug

# 或编译Release版本APK（优化后）
flutter build apk --release
```

### 步骤4：查看编译结果

```bash
# Debug版本
ls -lh build/app/outputs/flutter-apk/app-debug.apk

# Release版本
ls -lh build/app/outputs/flutter-apk/app-release.apk
```

### 步骤5：下载APK到本地

**在本地Mac终端运行：**

```bash
# 下载Debug版本
scp root@47.243.177.166:/root/app/build/app/outputs/flutter-apk/app-debug.apk ~/Desktop/app-debug-new.apk

# 或下载Release版本
scp root@47.243.177.166:/root/app/build/app/outputs/flutter-apk/app-release.apk ~/Desktop/app-release-new.apk
```

### 步骤6：安装到手机

```bash
# 检查设备连接
adb devices

# 安装Debug版本
adb install -r ~/Desktop/app-debug-new.apk

# 或安装Release版本
adb install -r ~/Desktop/app-release-new.apk
```

## 📝 快速命令序列

### 在服务器上（SSH后）：
```bash
cd /root/app
flutter pub get
flutter build apk --debug
ls -lh build/app/outputs/flutter-apk/app-debug.apk
```

### 在本地Mac：
```bash
scp root@47.243.177.166:/root/app/build/app/outputs/flutter-apk/app-debug.apk ~/Desktop/app-debug-new.apk
adb install -r ~/Desktop/app-debug-new.apk
```

## ✅ 验证

安装新版本后：
1. **应用详情 → 权限管理**：应该能看到"网络"权限（不再是灰色）
2. **应用功能**：应该能正常显示视频列表

---

**AndroidManifest.xml已上传！现在可以在服务器上编译了！**


