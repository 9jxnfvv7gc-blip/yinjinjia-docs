# 📋 备案所需信息说明

## Android 平台

### 1. 软件包名称（Package Name）
**值：** `com.example.video_music_app`

**位置：** `android/app/build.gradle.kts`

---

### 2. 公钥和证书 MD5 指纹

#### 方法1：直接提供 APK 包（推荐，最简单）
- 构建 release APK：`flutter build apk --release`
- APK 路径：`build/app/outputs/flutter-apk/app-release.apk`
- 直接上传这个 APK 文件即可

#### 方法2：从 APK 中提取证书信息
```bash
# 提取证书 MD5 指纹
keytool -printcert -jarfile build/app/outputs/flutter-apk/app-release.apk | grep -i MD5
```

#### 方法3：从签名文件中提取（如果有自定义签名）
```bash
# 如果有签名文件
keytool -list -v -keystore android/app/key.jks -alias your-alias
```

#### 方法4：使用默认调试签名
```bash
# 调试签名 MD5 指纹
keytool -list -v -keystore ~/.android/debug.keystore -alias androiddebugkey -storepass android -keypass android | grep -i MD5
```

---

## iOS 平台

### 1. Bundle ID
**值：** `com.xiaohui.videoMusicApp`

**位置：** `ios/Runner.xcodeproj/project.pbxproj`

---

### 2. 证书 SHA-1 指纹

#### 方法1：在 Xcode 中查看
1. 打开 Xcode
2. Xcode → Preferences → Accounts
3. 选择您的 Apple ID
4. 点击 "Manage Certificates"
5. 查看证书信息

#### 方法2：使用命令行
```bash
# 查看所有代码签名证书
security find-identity -v -p codesigning

# 查看特定证书的详细信息
security find-certificate -c "Apple Development: Your Name" -p | openssl x509 -fingerprint -sha1 -noout -in /dev/stdin
```

---

### 3. 公钥

公钥需要从证书中提取：
```bash
# 从证书中提取公钥
security find-certificate -c "Apple Development: Your Name" -p | openssl x509 -pubkey -noout
```

---

## 📝 当前项目信息

### Android
- **软件包名称：** `com.example.video_music_app`
- **证书 MD5：** 需要构建 release APK 后提取

### iOS
- **Bundle ID：** `com.xiaohui.videoMusicApp`
- **证书 SHA-1：** 需要在 Xcode 或命令行中查看

---

## 🔧 快速获取方法

### Android（最简单）
```bash
# 1. 构建 release APK
flutter build apk --release

# 2. 直接提供 APK 文件
# 文件路径：build/app/outputs/flutter-apk/app-release.apk
```

### iOS
```bash
# 查看证书 SHA-1
security find-identity -v -p codesigning | grep "iPhone Developer"
```

---

## 💡 建议

1. **Android：** 直接提供 APK 包最简单
2. **iOS：** 在 Xcode 中查看证书信息最方便
3. 如果还没有发布证书，需要先在 Apple Developer 或 Google Play Console 中创建
