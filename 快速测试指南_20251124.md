# 🚀 快速测试指南 - 2025-11-24

## 📱 1. Android真机测试

### 快速步骤：

```bash
# 1. 进入项目目录
cd /Volumes/Expansion/FlutterProjects/桌面影音播放器_安装包_20251121_165905

# 2. 检查设备连接
adb devices

# 3. 运行应用
flutter run -d android
```

### 详细步骤：

1. **启用USB调试**：
   - 手机设置 → 关于手机 → 连续点击"版本号"7次
   - 返回设置 → 开发者选项 → 启用"USB调试"

2. **连接手机**：
   - USB线连接手机和Mac
   - 手机上确认"允许USB调试"

3. **检查连接**：
   ```bash
   adb devices
   # 应该看到设备ID和"device"状态
   ```

4. **运行应用**：
   ```bash
   flutter run -d android
   ```

---

## 📱 2. iOS真机测试

### 快速步骤：

```bash
# 1. 进入项目目录
cd /Volumes/Expansion/FlutterProjects/桌面影音播放器_安装包_20251121_165905

# 2. 检查设备
flutter devices

# 3. 运行应用（需要Xcode配置）
flutter run -d ios
```

### 详细步骤：

1. **连接iPhone**：
   - 用USB线连接iPhone和Mac
   - 在iPhone上点击"信任此电脑"

2. **在Xcode中配置**：
   - 打开 `ios/Runner.xcworkspace`
   - 选择你的iPhone作为目标设备
   - 在"Signing & Capabilities"中选择你的开发团队
   - 确保Bundle Identifier唯一

3. **运行应用**：
   ```bash
   flutter run -d ios
   ```

4. **如果遇到签名问题**：
   - 在Xcode中：Runner → Signing & Capabilities
   - 选择你的Apple ID作为Team
   - 确保Bundle Identifier是唯一的（如：com.yourname.videoplayer）

---

## 💾 3. 如何保存文件以便快速找到

### 方法1：使用Git保存（推荐）

```bash
# 1. 进入项目目录
cd /Volumes/Expansion/FlutterProjects/桌面影音播放器_安装包_20251121_165905

# 2. 查看当前状态
git status

# 3. 添加重要文件
git add lib/config.dart
git add lib/main.dart
git add lib/simple_home_page.dart

# 4. 提交（使用有意义的描述）
git commit -m "保存测试版本：Android和iOS测试配置完成"

# 5. 查看提交历史
git log --oneline -10
```

### 方法2：创建书签文件

创建 `快速访问.md` 文件：

```bash
# 在项目根目录创建
cat > /Volumes/Expansion/FlutterProjects/桌面影音播放器_安装包_20251121_165905/快速访问.md << 'EOF'
# 快速访问指南

## 📁 重要文件位置

### 核心代码文件
- `lib/main.dart` - 应用入口
- `lib/config.dart` - 服务器配置（47.243.177.166:8081）
- `lib/simple_home_page.dart` - 主界面
- `lib/video_player_page.dart` - 视频播放页面

### 配置文件
- `pubspec.yaml` - 依赖配置
- `ios/Runner.xcworkspace` - iOS项目
- `android/app/build.gradle.kts` - Android配置

### 服务器文件
- `video_server.py` - 服务器代码
- 服务器地址：47.243.177.166:8081
- 服务器目录：/root/videos/

### Git版本
- 当前版本：fb56977 (2025-11-23)
- 查看版本：`git show fb56977`
- 切换版本：`git checkout fb56977`

## 🚀 快速命令

### 运行应用
```bash
cd /Volumes/Expansion/FlutterProjects/桌面影音播放器_安装包_20251121_165905
flutter run -d android  # Android
flutter run -d ios      # iOS
```

### 检查设备
```bash
flutter devices
adb devices  # Android
```

### 服务器管理
```bash
# 查看服务器状态
systemctl status video-server

# 重启服务器
systemctl restart video-server

# 查看日志
journalctl -u video-server -f
```

## 📝 测试检查清单

### Android测试
- [ ] USB调试已启用
- [ ] 设备已连接（adb devices）
- [ ] 应用成功安装
- [ ] 视频可以正常显示
- [ ] 上传功能正常
- [ ] 播放功能正常

### iOS测试
- [ ] iPhone已连接
- [ ] Xcode签名配置完成
- [ ] 应用成功安装
- [ ] 视频可以正常显示
- [ ] 上传功能正常
- [ ] 播放功能正常
EOF
```

### 方法3：使用Cursor工作区

1. **保存工作区**：
   - Cursor → File → Save Workspace As...
   - 保存为 `video_app.code-workspace`

2. **打开工作区**：
   - Cursor → File → Open Workspace
   - 选择保存的 `.code-workspace` 文件

### 方法4：创建快捷脚本

```bash
# 创建快速启动脚本
cat > ~/quick_test.sh << 'EOF'
#!/bin/bash
cd /Volumes/Expansion/FlutterProjects/桌面影音播放器_安装包_20251121_165905

echo "选择测试平台："
echo "1. Android"
echo "2. iOS"
echo "3. 查看设备"
read -p "请输入选项 (1-3): " choice

case $choice in
    1)
        echo "检查Android设备..."
        adb devices
        flutter run -d android
        ;;
    2)
        echo "检查iOS设备..."
        flutter devices
        flutter run -d ios
        ;;
    3)
        flutter devices
        adb devices
        ;;
    *)
        echo "无效选项"
        ;;
esac
EOF

chmod +x ~/quick_test.sh
```

使用：`~/quick_test.sh`

---

## 📋 测试检查清单

### Android真机测试
- [ ] USB调试已启用
- [ ] 设备已连接（`adb devices`显示设备）
- [ ] 应用成功编译
- [ ] 应用成功安装
- [ ] 视频列表正常显示
- [ ] 视频播放正常
- [ ] 上传功能正常
- [ ] 删除功能正常

### iOS真机测试
- [ ] iPhone已连接
- [ ] Xcode签名配置完成
- [ ] 应用成功编译
- [ ] 应用成功安装
- [ ] 视频列表正常显示
- [ ] 视频播放正常
- [ ] 上传功能正常
- [ ] 删除功能正常

---

## 🎯 快速访问命令

```bash
# 进入项目
cd /Volumes/Expansion/FlutterProjects/桌面影音播放器_安装包_20251121_165905

# Android测试
adb devices && flutter run -d android

# iOS测试
flutter devices && flutter run -d ios

# 查看Git版本
git log --oneline -5

# 切换到测试版本
git checkout fb56977

# 查看服务器状态
systemctl status video-server
```

---

**保存日期：2025-11-24**
**当前Git版本：fb56977**
**服务器地址：47.243.177.166:8081**


